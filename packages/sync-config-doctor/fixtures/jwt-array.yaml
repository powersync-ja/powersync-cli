streams:
  by_role:
    with:
      roles: |
        SELECT role_id FROM role_assignments
        WHERE role_id IN auth.parameter('roles')
    query: |
      SELECT * FROM role_resources WHERE role_id IN (SELECT role_id FROM roles)
