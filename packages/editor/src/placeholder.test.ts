import { describe, expect, it } from 'vitest';

// Placeholder test to keep the editor package participating in CI until real tests are added.
describe('editor placeholder', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
