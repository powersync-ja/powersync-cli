/**
 * Environment variables to be passed from the PowerSync CLI
 */
export const env = {
  /**
   * The path to the project's PowerSync config directory.
   */
  POWERSYNC_DIRECTORY: process.env.POWERSYNC_DIRECTORY!
};
