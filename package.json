{
  "name": "powersync-workspace",
  "private": true,
  "description": "PowerSync CLI monorepo",
  "devDependencies": {
    "@powersync/cli": "workspace:*",
    "prettier": "^3.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter @powersync/cli exec -- pnpm run dev",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.0.0"
}
